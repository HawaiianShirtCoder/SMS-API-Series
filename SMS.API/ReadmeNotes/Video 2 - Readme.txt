- Get the API to return all player data (in memory data to act as a database as we haven't got one yet)
- Get the API to return one player's data
- Quick and dirty approach - all done in the controller method (with a refactor in later videos)